name: GitHub Actions Demo
on: [push]
jobs:
  Explore-GitHub-Actions:
    runs-on: ubuntu-latest
    steps:
      - name: Initialize Python 2.7
        uses: actions/setup-python@v1
        with:
            python-version: 2.7
      - run: pip install -r ./TODOloo/backend/requirements.txt
      - run: pytest ./TODOloo/backend/test_backend.py
